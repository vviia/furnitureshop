import 'package:flutter/material.dart';

class BilingDetail extends StatelessWidget {
  const BilingDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
          child: const Center(
        child: Text('Biling Detail'),
      )),
    );
  }
}
